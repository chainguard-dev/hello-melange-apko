name: JavaScript

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/js.yml
      - js/**
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2

    # TODO: this workflow
